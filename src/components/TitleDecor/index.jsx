import styles from '../../styles/components/titleDecor.module.scss';
import clsx from 'clsx';

const TitleDecor = () => {
    return <div className={clsx(styles.container)}></div>;
};

export default TitleDecor;
